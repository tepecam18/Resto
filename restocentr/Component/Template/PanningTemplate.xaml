<UserControl 
    Name="userControl"
    x:Class="restocentr.Component.Template.PanningTemplate"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    mc:Ignorable="d"
    PreviewMouseDown="Grid_PreviewMouseDown"
    PreviewMouseMove="Grid_PreviewMouseMove"
    PreviewMouseUp="Grid_PreviewMouseUp">
    <ScrollViewer 
        Name="sv"
        SnapsToDevicePixels="true"
        VerticalScrollBarVisibility="{Binding IsVertical}"
        HorizontalScrollBarVisibility="{Binding IsHorizontal}">
        <ContentPresenter Content="{Binding AdditionalContent, ElementName=userControl}"/>
    </ScrollViewer>
</UserControl>
